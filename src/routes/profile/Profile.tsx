const styles = {
    profileContainer: {
        padding: "16px"
    }
};

export const Profile = () => {
    return (
        <div style={styles.profileContainer}>
            <h2>
                Profile
            </h2>
        </div>
    );
};
