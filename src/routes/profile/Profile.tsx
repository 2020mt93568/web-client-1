const styles = {
    profileContainer: {
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center"
    }
};

export const Profile = () => {
    return (
        <div style={styles.profileContainer}>
            Profile
        </div>
    );
};
