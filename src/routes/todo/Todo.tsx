const styles = {
    todoContainer: {
        padding: "16px"
    }
};

export const Todo = () => {
    return (
        <div style={styles.todoContainer}>
            <h2>
                Todo
            </h2>
        </div>
    );
};